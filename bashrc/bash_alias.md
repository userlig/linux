# Alias

```
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias c='clear'
alias update='sudo apt update && sudo apt upgrade && flatpak update'
alias x='exit'
alias install='sudo apt install'
alias weather='curl wttr.in/Santorini'
alias remove='sudo apt purge'
alias usergit='cd /home/userlig/Git/linux'
```